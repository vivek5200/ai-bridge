{
  "name": "@ai-bridge/browser-extension",
  "version": "1.0.1",
  "private": true,
  "description": "AI Bridge browser extension — capture AI code and send to VS Code",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@anthropic-ai/claude-code": "^2.1.92",
    "chrome-types": "^0.1.425",
    "rimraf": "^6.0.0",
    "typescript": "^5.3.0",
    "vite": "^6.0.0"
  }
}
